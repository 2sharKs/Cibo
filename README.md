# Cibo
A high-performance Native Android proof-of-concept logistics tracking pipeline built on an event-driven MVVM architecture. Leverages Kotlin Asynchronous Flows on isolated background worker pools (Dispatchers.IO) to handle real-time data streaming, paired with a type-safe enum state machine and reactive UI rendering using Jetpack Compose Material3.
